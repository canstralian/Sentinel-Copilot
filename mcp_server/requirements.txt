# Sentinel-Jedi MCP Server — Python dependencies
# Install with: pip install -r mcp_server/requirements.txt

# MCP framework
fastmcp>=2.0.0

# Azure authentication (supports DefaultAzureCredential chain:
# env vars → managed identity → CLI credentials → workload identity)
azure-identity>=1.17.0

# Async HTTP client (replaces sync requests for proper async MCP tools)
httpx>=0.27.0

# MITRE ATT&CK data access
# mitreattack-python provides the MitreAttackData class for local STIX bundle queries
mitreattack-python>=3.0.0

# attackcti provides a live TAXII 2.1 client against MITRE's CTI server
# Used for threat-actor TTP lookups without a local STIX file
attackcti>=0.4.0
